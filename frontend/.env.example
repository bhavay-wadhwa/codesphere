# URL of the Express API during local development. Leave this unset in the single-service
# Render deployment, where the frontend and backend use the same origin.
VITE_API_URL=http://localhost:4000

# Google OAuth Web Application client ID. This value is public by design; never put the
# corresponding client secret in the frontend environment.
VITE_GOOGLE_CLIENT_ID=
